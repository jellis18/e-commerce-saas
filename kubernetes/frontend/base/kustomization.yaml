apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: proshop

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - literals:
      - API_BASE_URL=http://proshop-api.proshop.svc.cluster.local:8000
    name: proshop-ui
    namespace: proshop
